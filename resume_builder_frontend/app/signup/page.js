import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - Resume Builder',
  description: 'Create a new resume builder account',
};

export default function SignupPage() {
  return <SignupForm />;
}
